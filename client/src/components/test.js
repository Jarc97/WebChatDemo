
/*
import React from 'react';

// Recommended way to handle components
// Using arrow functions (lambdas)
const Test = (props) => (
    <h3>{props.title}</h3>
);


// Using functions
const Test = function test (props) {
    return <h3>{props.title}</h3>
}


// Using classes
class Test extends React.Component {
    constructor(props) {
        super(props);
        this.title = props.title;
        this.state = {title: props.title};
    }

    render() {
        return (
            <h3>{this.title}</h3>
        );
    }
}

export default Test;
*/